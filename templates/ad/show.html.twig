{% extends "base.html.twig" %}

{% block title %}
{{ ad.title }}
{% endblock %}

{% block body %}
<div class="ad-title" style="background-image: url({{ad.coverImage}})">
    <div class="container">
        <h1>{{ad.title}}</h1>
        <p>{{ ad.introduction}}</p>
        <p class="h2">
            <strong>{{ad.rooms}} chambres</strong> pour <strong>{{ad.price}}&euro;</strong> par nuit
        </p>
        <a href="#" class="btn btn-primary">Réserver !</a>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-md-8 mb-3">
            {{ad.content|raw}}
            <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                <ol class="carousel-indicators">
                    {% for image in ad.images %}
                        <li data-target="#carouselExampleIndicators" data-slide-to="{{loop.index0}}" class="{% if loop.first %}active{% endif %}"></li>
                    {% endfor %}
                </ol>
                <div class="carousel-inner">
                    {% for image in ad.images %}
                    <div class="carousel-item {% if loop.first %}active{% endif %}">
                        <img src="{{image.url}}?{{ ad.slug ~ random(1,5000000) }}" class="d-block w-100" alt="{{image.caption}}">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>{{ad.title}}</h5>
                            <p>{{image.caption}}</p>
                          </div>
                    </div>
                    {% endfor %}
                </div>
                <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        </div>
        <div class="col">
            <div class="row">
                <div class="col-4">
                    <img src="http://placehold.it/64x64" alt="Avatar de Joseph Dupont">
                </div>
                <div class="col">
                    <h3>Joseph Dupont</h3>
                    <span class="badge badge-primary">3 annonces</span>
                </div>
            </div>
            <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Possimus, est quo fuga in fugiat veritatis iure
                dolores? Velit, placeat voluptas?</p>
            <p>Pariatur eum officiis voluptatem, consequuntur modi repudiandae velit non illum vel, optio sit. Quia
                molestiae architecto deserunt sequi, sint accusamus.</p>
            <p>Dicta ipsum dolores facilis neque ad? Unde inventore, corrupti qui quam quod assumenda soluta quo
                expedita minus nam a? Molestias.</p>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
<script>
    $('.carousel').carousel({
        interval: 2000
      })
</script>
{% endblock %}